SUPABASE_URL=ton_url_supabase_ici
SUPABASE_KEY=ta_cle_anon_ou_service_ici

# ── LLM (proxy Netlify + dev local) ──
# LLM_API_URL=https://api.deepseek.com/chat/completions
# LLM_API_KEY=ta_cle_llm_ici (JAMAIS commitée, jamais côté client)
# LLM_MODEL=deepseek-chat

# ── Quotas API anti-fuite (serveur UNIQUEMENT : Netlify → Environment variables) ──
# NE JAMAIS exposer ces valeurs côté client (js/ ne les lit pas).
# SUPABASE_SERVICE_ROLE_KEY=ta_cle_service_role_ici (proxy : RPC llm_gate / log_llm_usage)
# QUOTA_SALT=une_chaine_aleatoire_longue (sel du hash SHA-256 des IP, RGPD : aucune IP en clair)
# QUOTA_CONTACT_EMAIL=hamlat.louai@gmail.com (mailto affiché à quota épuisé)
